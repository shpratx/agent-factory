models:
  - type: main
    engine: openai
    model: gpt-4.1

rails:
  output:
    flows:
      - check audit trail completeness  # LLM validates fields (pure Colang)
      - log audit record                # Python persists record (actions.py)

# Persistence: defaults to stdout logging.
# Uncomment the relevant section in actions.py for production stores.
