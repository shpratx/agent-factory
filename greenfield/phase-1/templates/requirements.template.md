<!--
TEMPLATE: requirements.md
Produced by: L1-requirements-elicitor (Core)
Evaluated by: L1-requirements-elicitor-evaluator
Consumes: vision.md (APPROVED — must carry a recorded Product Lead approval
          comment; this agent refuses to run without one, per its NFR delta)
Consumed by: L1-requirements-nfr-classifier, L1-requirements-prd-composer,
             L1-inception-story-generator (Phase 3, direct) — NOTE:
             L1-planning-impact-assessor and L1-planning-dependency-mapper no
             longer consume this directly; they consume prd.md instead
             (see prd.template.md)

Required sections marked ✅. Every requirement MUST trace to exactly one
vision.md clause — cite it by section name, not paraphrase-only. If a vision
clause bundles two independently testable capabilities into one sentence
(e.g. "fast AND cheap"), split it into two requirements and say so explicitly
— do not pass the compound statement through as a single requirement.
-->

# Requirements: {{product_name}}

| Field | Value |
|---|---|
| Source vision | `vision.md` ({{vision_artifact_id}}) |
| Approval consumed | {{approver_name_role}}, {{yyyy-mm-dd}}: "{{approval_comment_text}}" |
| Generated | {{yyyy-mm-dd}} |

## Compound Requirements Split
{{list any vision.md clause that bundled ≥2 independently testable capabilities
into one sentence, and which requirement IDs it was split into. Leave as
"None — no compound clauses found" only if genuinely true.}}

## ✅ Functional Requirements

### FR-{{NNN}}: {{short title}}
**Statement:** {{single, atomic, testable capability — no "and" joining two
different behaviours}}
**Traces to:** vision.md § {{section name}}
**Notes:** {{only if this FR resulted from splitting a compound clause}}

{{repeat one block per requirement}}

---
*Generated by `L1-requirements-elicitor` · execution_id: `{{execution_id}}` · workflow_execution_id: `{{workflow_execution_id}}`*
